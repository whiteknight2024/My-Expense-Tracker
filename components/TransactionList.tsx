import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Typo from "./Typo";

const TransactionList = () => {
  return (
    <View>
      <Typo>TransactionList</Typo>
    </View>
  );
};

export default TransactionList;

const styles = StyleSheet.create({});
